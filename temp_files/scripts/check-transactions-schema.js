const { createClient } = require("@supabase/supabase-js"); const supabaseUrl = "https://tbuqctfgjjxnevmsvucl.supabase.co"; const supabaseKey = process.env.VITE_SUPABASE_ANON_KEY || "your-anon-key"; const supabase = createClient(supabaseUrl, supabaseKey); async function checkTransactionsSchema() { console.log("🔍 Vérification du schéma de la table transactions..."); try { const { data: sampleData, error: sampleError } = await supabase.from("transactions").select("*").limit(1); if (sampleError) { console.log("❌ Erreur:", sampleError.message); } else if (sampleData && sampleData.length > 0) { console.log("✅ Colonnes disponibles:", Object.keys(sampleData[0])); } else { console.log("ℹ️ Aucune donnée dans la table"); } } catch (error) { console.error("❌ Erreur:", error); } } checkTransactionsSchema().catch(console.error);
